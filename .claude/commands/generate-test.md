Read the `problem.md` and `solution.py` in the specified problem directory under `problems/`.

Generate a `test_solution.py` file with:
1. Import the solution function(s) from `solution` using absolute import: `from solution import function_name`
2. Use `pytest` (plain assert statements, no unittest)
3. Include all example cases from `problem.md`
4. Add 2-3 edge cases (empty input, single element, large values, boundary conditions, etc.)
5. Write test function names in format: `test_example_1`, `test_edge_empty`, etc.
6. Add a brief comment on each test describing the case

Only output the test file — do not modify `problem.md` or `solution.py`.

$ARGUMENTS
